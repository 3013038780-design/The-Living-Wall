# 近墙区域诊断（Refs #25）

## Why
单一最大区域中心不能解释手掌、前臂和上半身的空间分布。

## What Changes
显示多个几何前景轮廓和独立近墙区域，给出法向间距和深度覆盖率。保留旧接触候选指标并明确其含义。

## Non-goals
不宣称人体语义分割、身份追踪、真实触碰或投影映射完成；不改线上碎光。

## Impact
仅 tools/depth-lab；新增本地诊断字段。
